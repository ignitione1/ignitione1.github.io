version: '3.8'

services:
  portfolio:
    build: .
    container_name: portfolio
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
