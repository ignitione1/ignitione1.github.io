# Продакшен-переменные сборки. Это НЕ секреты — попадают в публичный бандл.
# Токен бота сюда НЕ кладём, он живёт в env Vercel.
#
# URL serverless-функции контактной формы (Vercel-проект revyakin-contact-api).
# После деплоя функции на Vercel впиши сюда реальный URL проекта.
VITE_CONTACT_API_URL=https://revyakin-contact-api.vercel.app/api/contact
